name: CI for WASM on macOS

on:
  push:
    branches: [ "main" ]
    paths:
      - 'include/**'
      - 'src/**'
      - 'third-party/**'
      - 'cmake/**'
      - 'CMakeLists.txt'
      - 'CMakePresets.json'
      - 'vcpkg.json'
      - 'build-tools/build-utils.sh'
      - 'build-tools/build-WebAssembly.sh'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'

    - name: Setup Emscripten
      uses: mymindstorm/setup-emsdk@v12
      with:
        version: 3.1.65

    - name: Setup CMake & Ninja
      run: |
        brew install cmake ninja | true

    - name: Setup vcpkg
      uses: lukka/run-vcpkg@v11
      with:
        runVcpkgInstall: false

    - name: Setup ccache
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: wasm-ccache-${{ runner.os }}
        max-size: 888M

    - name: Build
      env:
        VCPKG_ROOT: ${{ github.workspace }}/vcpkg
        VCPKG_BINARY_SOURCES: "default;x-gha,readwrite"
        EMSCRIPTEN_ROOT: ${{ env.EMSDK }}/upstream/emscripten
      run: |
        cd build-tools
        chmod +x build-WebAssembly.sh
        ./build-WebAssembly.sh

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: WebAssembly-build
        path: |
          build.Wasm/Release/output/
        if-no-files-found: error
