name: CI-WASM-Mac

on:
  push:
    branches: [ "main" ]
    paths:
      - 'include/**'
      - 'src/**'
      - 'cmake/**'
      - 'CMakeLists.txt'
      - 'CMakePresets.json'
      - 'vcpkg.json'
      - 'build-tools/utils.ts'
      - 'build-tools/setup-vcpkg.ts'
      - 'build-tools/build-WASM.ts'
      - '.github/workflows/Base-Setup.yml'
      - '.github/workflows/CI-WASM-Mac.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  baseSetup:
    uses: ./.github/workflows/Base-Setup.yml
    with:
      host: macOS
      target: wasm

  build:
    needs: baseSetup
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'

    - name: Setup Emscripten
      uses: mymindstorm/setup-emsdk@v12
      with:
        version: 3.1.65

    - name: Setup custom vcpkg
      run: |
        cd build-tools
        npx tsx setup-vcpkg.ts

    - name: Cache vcpkg binary
      id: cache-vcpkg
      uses: actions/cache@v4
      with:
        path: ${{ env.HOME }}/vcpkg-binary-cache
        key: vcpkg-binary-cache-wasm-${{ runner.os }}-${{ hashFiles('vcpkg.json') }}

    - name: Build
      env:
        CI_WASM_SDK_HOME: ${{ env.EMSDK }}
      run: |
        cd build-tools
        npx tsx build-WASM.ts

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: WASM-Mac-build
        path: |
          build.Wasm/Release/output/
        if-no-files-found: error
